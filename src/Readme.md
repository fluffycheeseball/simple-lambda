# Building

` cd src

` go build .


# Docker Testing

Build our Dockerfile image
`docker-compose build`

Run the container
`docker-compose up`


Stop the container
`docker-compose down`


